<html>
<head>
    <title>@ViewBag.Title 我的首页</title>
    <script src="@Url.Content("~/scripts/jquery-1.10.2.js")"></script>
    <script src="@Url.Content("~/scripts/bootstrap-3.3.2-dist/js/bootstrap.js")"></script>
    <link href="@Url.Content("~/scripts/bootstrap-3.3.2-dist/css/bootstrap.css")" rel="stylesheet" />
    <style>
        .containerDiv {
            margin-left: 0;
            padding: 60px 0 0 0;
        }

        .body {
            color: #666;
            font-family: "微软雅黑";
        }

        .registe {
            margin-left: -30px !important;
            margin-right: -31px;
        }
    </style>


</head>
<body>
    <div class="container-fluid">
        <div class="pageNavigation">
            <div class="row">
                <nav class="navbar-header navbar-default navbar-fixed-top head">
                    <a class="navbar-brand" href="@Url.Action("Index","Home")">首页</a>
                    <a class="navbar-brand" href="#">新闻</a>
                    <a class="navbar-brand" href="#">社会</a>
                    <a class="navbar-brand" href="#">关于我们</a>
                    @{
                        if (Session["userInfo"] == null)
                        {
                            <a class="navbar-brand pull-right" href="@Url.Action("Index", "Login")">登陆</a>
                        }
                        else
                        {
                            //var userInfo = (UserInfo)Session["userInfo"];
                            @*<a class="navbar-brand pull-right " href="@Url.Action("LogOut", "LogOut")"> |注销</a>
                            <a class="navbar-brand pull-right registe" href="@Url.Action("Index", "UserInfomation")">@{ @userInfo.UserName }</a>*@
                                }
                    }
                </nav>
            </div>
        </div>

        <div class="container-fluid containerDiv">
            @RenderBody()
        </div>

        <div class="bottom">
            <div class="row">
                <nav class="navbar navbar-default navbar-fixed-bottom bottonFont">
                    <div>
                        <span class="col-lg-offset-5">版权所有：朝阳区</span>
                    </div>
                </nav>
            </div>
        </div>


    </div>



</body>

</html>

@RenderSection("script", required: false)
